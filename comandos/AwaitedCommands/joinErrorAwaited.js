module.exports.awaitedCommand = {
  name: "joinError",
  code:
`
$author[$userTag;$authorAvatar] $color[8000FF] $description[<:error:823299033011191808> **ERROR!** Como usar: \`$getServerVar[prefixo]join-message ( mensagem )\`

  **Funções:**
- \`{user.tag}\` retorna a tag do usúario
- \`{user.mention}\` menciona o usuário
- \`{user.username}\` retorna o nome do usuário
- \`{server.name}\` retorna o nome do servidor
- \`{server.members}\` retorna o total de membros do servidor]
`
}